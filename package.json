{
  "proxy": "http://localhost:3001/",
  "name": "iiitd-chat",
  "version": "1.0.0",
  "engines":{
    "node":"16.x"
  },
  "description": "iiitd chat app for DBMS",
  "main": "index.js",
  "scripts": {
    "production": "npm run client && npm run server",
    "start": "npm run start --prefix Backend",
    "build": "npm run build --prefix Frontend ",
    "test": "start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.1.0"
  }
}
